# Bucket 设置界面说明

Bucket 管理已整合进现有“设置 → R2 设置”页签。

设计原则：

- 顶部仅保留 Bucket 快速切换器；
- 不再使用独立管理弹窗和额外齿轮按钮；
- 添加、删除、连接测试和 CORS 配置统一放在 R2 设置中；
- 复用项目已有的 `field`、`field-label`、`btn`、设计变量和响应式断点；
- 切换 Bucket 后刷新页面，避免缓存、上传和预览上下文跨 Bucket 复用。

当前 Bucket 的名称、自定义域名和链接模式继续通过设置页底部“保存”按钮保存；显示名称由同一次保存操作同步更新。
