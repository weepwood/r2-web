# AGENTS.md

本文件是 AI Agent 在 r2-web 仓库中工作的强制入口。r2-web 是浏览器端 Cloudflare R2 文件管理工具，直接处理 Bucket、对象、API Token、预签名 URL、CORS、上传下载与多 Bucket 切换。任何修改都必须优先考虑凭据安全、对象误操作、浏览器限制和真实 R2 行为。

## 1. 修改前必读

按顺序读取：

1. 当前 Issue、PR 描述、验收标准和明确非目标；
2. `README.md` 与 `docs/` 中的多 Bucket、CORS、权限和部署说明；
3. 本文件；
4. 相关 R2 客户端、签名、Bucket 配置、文件浏览、上传下载和测试实现。

修改前先确认：

- 当前操作针对哪个 Bucket；
- 是否会删除、覆盖、移动或批量修改真实对象；
- 是否改变 Token、Account ID、Bucket、CORS 或预签名链接边界；
- 是否影响写操作期间的 Bucket 切换与失败回滚；
- 是否需要真实 Cloudflare 账户验证。

## 2. 核心不变量

- API Token、Secret、Account ID 和私有对象地址不得进入代码、测试、日志、截图、Issue、PR 或 Artifact；
- 浏览器本地配置不是安全凭据保险箱，界面必须明确其本地存储边界；
- 多 Bucket 配置只保存在浏览器本地，不代表修改 Cloudflare 端真实 Bucket；
- 删除本地 Bucket 配置不得删除云端真实 Bucket；
- Bucket 切换不能在上传、复制、移动、删除、Content-Type 修改或创建文件夹期间发生；
- 切换失败必须保留当前 Bucket 和当前文件列表；
- 不跨 Bucket 复用对象列表、预览、上传上下文或写操作状态；
- 公开域名与预签名链接只是链接生成方式，不等同于修改对象公开权限；
- CORS 配置不会扩大 API Token 权限；
- 浏览器端 R2 请求、SigV4、预签名 URL 与 Cloudflare 权限必须分别处理。

## 3. 风险等级

- **P0**：凭据泄露、跨 Bucket 误写、批量误删、对象覆盖、公开私有边界失效；
- **P1**：签名、Token 权限、Bucket 切换、上传下载、复制移动、删除、CORS 和部署；
- **P2**：多 Bucket UI、缓存、预览、分页、性能和浏览器兼容；
- **P3**：低风险 UI、文档、测试和内部整理。

P0/P1 必须有关联 Issue、失败路径测试、独立审查和可执行回滚。

## 4. 强制工作流

### 调查

先只读并确认：

- Bucket 配置、当前 Bucket、R2 客户端和签名调用链；
- 文件浏览、上传、下载、复制、移动和删除状态；
- CORS、Token、预签名链接和公开域名影响；
- 缓存、预览和对象列表是否可能跨 Bucket 泄漏；
- 已确认事实、推断、未知项和最小修改范围。

### 实现

- 一个 PR 只解决一个主要问题；
- 不混合功能、无关重构、依赖升级和全局格式化；
- 优先复用现有 Bucket 管理、R2 客户端、签名和错误处理机制；
- 不建立第二套 Bucket 配置、客户端、缓存或上传状态；
- 不为局部需求引入后端服务或长期凭据存储；
- 提交说明使用中文；
- 不直接推送 `main`，默认 squash merge。

### 测试先行

以下改动优先先写失败测试：

- Bucket 添加、编辑、删除、重复名称和当前 Bucket 保护；
- 切换成功、连接失败、403、404、网络错误和回滚；
- 写操作期间禁止切换；
- SigV4 查询参数、过期时间、Content-Type 和特殊字符；
- 上传覆盖、复制、移动、批量删除和部分失败；
- CORS 诊断、实际 Origin 和 Token 未授权 Bucket；
- 超长 Key、Unicode、空目录、分页和对象缓存；
- 公开域名、预签名链接和 URL 编码。

Mock 和静态测试不能替代真实 R2 Bucket、Token 权限和浏览器 CORS 验证。

## 5. 凭据与网络安全

- 不在前端日志和错误提示中输出完整 Token、签名、Authorization 或敏感查询参数；
- 示例配置只能使用虚构 Account ID、Bucket、Token 和域名；
- 错误信息应区分 403、404、CORS、网络不可读响应和 Token 未授权，不得把所有 `Failed to fetch` 断言为 CORS；
- Token 必须遵循最小权限，只授权用户实际管理的 Bucket；
- 推荐 CORS 规则必须使用当前页面精确 Origin；
- 不默认允许 `*` Origin 与高权限写操作；
- 预签名 URL 必须有明确过期时间和操作范围；
- 非 HTTPS 部署不得处理真实生产凭据。

## 6. 对象写操作

- 删除、覆盖、移动和批量操作必须有明确确认、对象范围和结果统计；
- 移动应在复制成功后再删除源对象，并处理部分成功状态；
- 写操作失败不得切换 Bucket 或清空当前上下文；
- 不把对象不存在与权限不足混为一谈；
- 大文件上传必须有进度、取消、失败重试和内存边界；
- 目录只是 Key 前缀，不得假设真实文件夹事务；
- 对象 Key 规范化不得静默改变用户路径语义。

## 7. 多 Bucket 边界

- 当前 Bucket 是所有对象操作的显式上下文；
- 切换前对目标 Bucket 做最小连接验证；
- 切换成功后回到目标 Bucket 根目录并清理旧预览和列表缓存；
- 切换失败恢复原 Bucket，不刷新整页、不丢失当前列表；
- 删除当前 Bucket 配置前必须先成功切换到备用 Bucket；
- 添加 Bucket 验证失败时可保留本地草稿，但不得改变当前活动 Bucket；
- 不实现自动 ListBuckets 同步，除非重新设计权限和 CORS 模型并单独审查。

## 8. 完成验证

完成前提供实际证据：

- `pnpm test`；
- `pnpm typecheck`；
- 生产构建；
- 部署预览；
- 真实 R2 账户中的读取、上传、下载和连接测试（适用时）；
- 多 Bucket 切换、失败回滚和写操作保护；
- 实际页面 Origin 的 CORS 验证；
- 未验证的 Token、Bucket 或浏览器行为和剩余风险。

CI 通过不能替代真实 Cloudflare 权限、浏览器 CORS 和对象写操作验证。